sudo yum update -y
sudo amazon-linux-extras install ansible2 or yum install ansible -y
---	
both side master/host:
create user : sudo useradd shameer(master) 
                       sudo passwd shameer(master)
                        sudo visudo , add this line 
				shameer    ALL=(ALL)   NOPASSWD: ALL(master)
			sudo useradd khan(webserver) 
                       sudo passwd khan(webserver)
				khan    ALL=(ALL)   NOPASSWD: ALL(werbserver)
				:wq 
			           sudo vi /etc/ssh/sshd_config
				update passwordauthentication yes  , :wq
			sudo systemctl restart sshd
  master: 
   su - shameer
   ssh-keygen , enter enter  enter
   cd /home/shameer/.ssh/
ssh-copy-id -i id_rsa.pub khan@10.0.3.223    
setup inventory file :
sudo vi /etc/ansible/hosts
[webserver]
ipaddres-of-webserver
:wq
 lab setup : launch 3 servers, master - web1,web2
 crossh check :
 ansible webserver -m ping
output ....ping pong
test command : ansible webserver -m yum -ba "pkg=git state=present"
ansible webserver -m yum -ba "pkg=docker state=present"
 ansible webserver  -ba "touch file1 file2"   


sudo ssh -i ansible.pem master@172.31.69.83             